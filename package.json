{
  "name": "trip-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate:deploy": "npx prisma migrate deploy",
    "migrate:start": "npx prisma migrate dev --name init",
    "start": "npm run migrate:start && nodemon src/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.11.0",
    "express": "^4.18.3",
    "nodemon": "^3.1.0",
    "prisma": "^5.11.0"
  }
}
